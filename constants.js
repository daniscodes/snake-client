const IP = '192.168.2.53';
const PORT = 50541;


const msg = "Say: ";
const hello = "Hello there!";
const good = "Nice!";
const bad = "Gotcha!";

const UPKEY = "Move: up";
const LEFTKEY = "Move: left";
const DOWNKEY = "Move: down";
const RIGHTKEY = "Move: right";

module.exports = {
  IP,
  PORT,
  msg,
  hello,
  good,
  bad,
  UPKEY,
  LEFTKEY,
  DOWNKEY,
  RIGHTKEY
};